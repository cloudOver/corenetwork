compatibility: whitetank
totem {
  version: 2
  secauth: off
  threads: 0
  # Note, fail_recv_const is only needed if you're
  # having problems with corosync crashing under
  # heavy sheepdog traffic. This crash is due to
  # delayed/resent/misordered multicast packets.
  # fail_recv_const: 5000
  {% for network in networks %}
    {% if network != '169.254.0.0' %}
      interface {
        ringnumber: {{ forloop.counter|add:"-1" }}
        bindnetaddr: {{ network }}
        mcastaddr: 226.94.1.1
        mcastport: {{ forloop.counter|add:"5404" }}
      }
    {% endif %}
  {% endfor %}
}
logging {
  fileline: off
  to_stderr: yes
  to_logfile: yes
  to_syslog: yes
  # the pathname of the log file
  logfile: /var/log/corosync.log
  debug: on
  timestamp: on
  logger_subsys {
    subsys: AMF
    debug: off
  }
}
amf {
  mode: disabled
}